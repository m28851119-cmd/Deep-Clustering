2026/03/22
对比不同的华为方案在CIFAR-10数据集上的效果，
1.环境与数据准备（基础垫层）
设备车辆：优先用GPU加速（Colab训练中默认支持），保证效率；
数据结构：对CIFAR-10测试集做标准化（符合CNN输入要求），加载为批量数据；
评估函数设计：用「匈牙利算法」解决皇冠标签与真实标签的匹配问题（皇冠结果的标签是随机的，需先匹配再计算准确率），同时计算NMI（归一化互信息）作为辅助评估指标；
原始数据提取：将图像展平为一维处理，提供传统的K-means/PCA使用。
2. 传统方案（基准对比）
方案一（原始像素K-means）：直接对展平后的像素提供K-means噪声，验证「原始像素特征」的戴维斯效果（预期效果差，因为像素级特征声音、噪声大）；
方案二（PCA+K-means）：先通过PCA将高维像素使降维到50维（减少发音），再做K-means，验证「简单降维」对奥迪的提升（比方案一好，但仍有限）。
3.深度方案（核心创新）
DEC（Deep Embedded Clustering）的核心是「用自编码器学习数据的低维嵌入，再在嵌入空间做噪音优化」，分3个关键步骤：
步骤A：预训练自编码器
利用重构损失（MSE）让编码器学习CIFAR-10的有效低维嵌入（128 维），相比手动降维（PCA），能学习到重构的特征；
步骤B：初始化中心
利用预训练编码器提取出所有数据的嵌入向量，再用K-means对嵌入向量，得到最初的嵌入向量（保证嵌入向量的核心价值有意义）；
步骤C：联合优化核心层与编码器
定义「迭代层」：基于Student-t分布计算每个样本属于各迭代的概率（Q分布）；
构造「目标分布P」：对Q分布做加权处理，让高置信度样本的权重更高（强化强化效果）；
损失优化：用KL散度最小化Q和P的差异，同时逆向传播更新编码器和增量层参数（让嵌入向量既保留重构信息，又满足增量结构）；
步骤D：最终评估：用优化后的编码器提取特征，随后层输出最终结果，计算ACC/NMI。
