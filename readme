参考深度聚类，选取数据集进行复现并对比传统聚类和深度聚类。
版本一不足（深度聚类）：
1. 自编码器设计太简单，无法提取 CIFAR-10 的有效特征
编码器仅 3 层卷积 + 1 层全连接，没有 BatchNorm、Dropout 等正则化，也没有残差连接，对 CIFAR-10 的复杂特征捕捉能力极弱；
2. 训练策略严重不足
预训练仅 50 个 Epoch
联合优化阶段用 SGD，lr=0.01（学习率太高），且仅 30 个 Epoch，模型震荡无法收敛；
3. DEC 核心参数
聚类层的 Student-t 分布中alpha=1，而 DEC 论文中 alpha=1.0 会导致软分配概率太 “硬”，失去 DEC 的软聚类优势（正确值应为 1.5~2.0）；
4. 数据预处理适配性差
CIFAR-10 的归一化是针对 RGB 图像的，但自编码器解码器输出 Tanh，和输入的归一化分布（均值～0.5，标准差～0.2）不匹配，重构损失无意义；
没有数据增强（如随机裁剪、翻转），CIFAR-10 训练数据量不足，模型泛化能力差。
